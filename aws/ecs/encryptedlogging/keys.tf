# https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/kms_key

resource "aws_kms_key" "cluster_key" {
  description             = "symmetric key used for ECS Cluster encryption configuration"
  enable_key_rotation     = true
  deletion_window_in_days = 20
  tags                    = local.common_tags
}

resource "aws_kms_alias" "cluster_key_alias" {
  name          = "alias/${var.ENVIRONMENT}/${var.APPLICATION_ID}/${local.cluster_name}"
  target_key_id = aws_kms_key.cluster_key.id
}

resource "aws_kms_key_policy" "key_policies" {
  key_id = aws_kms_key.cluster_key.id
  policy = jsonencode({
    Version = "2012-10-17"
    Id      = "key-default-1"
    Statement = [
      {
        Sid    = "Enable IAM User Permissions"
        Effect = "Allow"
        Principal = {
          AWS = "arn:aws:iam::${data.aws_caller_identity.current.account_id}:root"
        },
        Action   = "kms:*"
        Resource = "*"
      },
      {
        Sid    = "Allow administration of the key"
        Effect = "Allow"
        Principal = {
          AWS = "arn:aws:iam::${data.aws_caller_identity.current.account_id}:user/psharpx"
        },
        Action = [
          "kms:ReplicateKey",
          "kms:Create*",
          "kms:Describe*",
          "kms:Enable*",
          "kms:List*",
          "kms:Put*",
          "kms:Update*",
          "kms:Revoke*",
          "kms:Disable*",
          "kms:Get*",
          "kms:Delete*",
          "kms:ScheduleKeyDeletion",
          "kms:CancelKeyDeletion"
        ],
        Resource = "*"
      },
      {
        Sid    = "Allow use of the key"
        Effect = "Allow"
        Principal = {
          AWS = "arn:aws:iam::${data.aws_caller_identity.current.account_id}:user/crivera"
        },
        Action = [
          "kms:DescribeKey",
          "kms:Encrypt",
          "kms:Decrypt",
          "kms:ReEncrypt*",
          "kms:GenerateDataKey",
          "kms:GenerateDataKeyWithoutPlaintext"
        ],
        Resource = "*"
      }
    ]
  })
}